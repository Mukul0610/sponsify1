"use client"

import React from 'react'

const Pyment = () => {
  return (
    
      <div className='flex gap-4 mr-16 justify-center'>
              <button type="button" className='font-bold bg-green-700 text-dark-600 px-6 rounded-full m-2' onClick={()=>alert("Sorry, but this functionality is blocked since it is in testing mode")} >Add Credit</button>
              <button type="button" className='font-semibold bg-blue-700 text-dark-600 px-6 rounded-full m-2' onClick={()=>alert("Sorry, but this functionality is blocked since it is in testing mode")}>Withdrow</button>
            </div>
    
    
  )
}

export default Pyment
